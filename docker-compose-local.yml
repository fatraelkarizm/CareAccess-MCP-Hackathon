name: po-community-mcp

services:
  python:
    restart: always
    build:
      dockerfile: ./python/Dockerfile
    ports:
      - "55002:5001"
  typescript:
    restart: always
    build:
      dockerfile: ./typescript/Dockerfile
    ports:
      - "55000:5000"
